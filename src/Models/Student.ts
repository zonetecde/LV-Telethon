export default class Student {
	prenom: string;
	nom: string;
	classe: string;

	constructor(prenom: string, nom: string, classe: string) {
		this.prenom = prenom;
		this.nom = nom;
		this.classe = classe;
	}
}
